- provide(:title, @journal.name)
%h2
  = @journal.name
%p
  = @journal.description

- @journal.posts.each do |post|
  %article
    .time
      %i
        = post.time
    .text
      != post.text
    .image
      - if post.image_file_size
        = image_tag(post.image.url)
