- provide(:title, @journal.name)
%h2
  = @journal.name
%p
  = @journal.description

- @journal.posts.each do |post|
  %article
    .time
      %i
        = post.time
    .text
      != post.text
    .image
      = link_to(image_tag(post.image.url), post.image.url)
